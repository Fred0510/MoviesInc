import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: "100%",
    },
    headerTitle: {
        fontSize: 10, //large
        color: '#FFFFFF'
    }
});

export default styles;